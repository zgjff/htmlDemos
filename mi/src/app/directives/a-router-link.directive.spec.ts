import { ARouterLinkDirective } from './a-router-link.directive'

describe('ARouterLinkDirective', () => {
	it('should create an instance', () => {
		// @ts-ignore
		const directive = new ARouterLinkDirective()
		expect(directive).toBeTruthy()
	})
})
